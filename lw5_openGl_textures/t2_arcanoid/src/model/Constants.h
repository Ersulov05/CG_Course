#pragma once
#include "../common/Geometry.h"

inline static const Size3D SCENE_SIZE = Size3D{2, 1, 3};

inline static const float DEFAULT_BALL_RADIUS = 0.05;
inline static const float DEFAULT_BALL_SPEED = 1.5;
inline static const Point3D DEFAULT_BALL_POSITION = Point3D{0, 0, -0.2};

inline static const Size3D BLOCK_SIZE = Size3D{0.2, 0.2, 0.2};

inline static const Size3D DEFAULT_RACKET_SIZE = Size3D{0.4, 0.1, 0.1};
inline static const float DEFAULT_RACKET_SPEED = 2;